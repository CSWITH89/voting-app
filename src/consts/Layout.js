import {Dimensions} from 'react-native';

const SCREEN_WIDTH = Dimensions.get('screen').width;
const SCREEN_HEIGHT = Dimensions.get('screen').height;

const COLUMNS = 2;

export {SCREEN_WIDTH, SCREEN_HEIGHT, COLUMNS};
